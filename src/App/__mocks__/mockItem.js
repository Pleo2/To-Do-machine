// eslint-disable-next-line import/prefer-default-export
export const mockItem = {
  complete: true,
  unComplete: false,
  id: 11111,
  index: 0,
  text: 'make test for item',
  onComplete: jest.fn(),
  onDelete: jest.fn(),
}